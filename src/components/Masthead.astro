<div class="masthead-wrap">
  <div class="utility-line" aria-label="Publication details">
    <span>Vol. 1 · No. 1</span>
    <span>Independent &amp; brined daily</span>
    <span>Price: one minute</span>
  </div>
  <a class="masthead" href="/" aria-label="The Pickle home">
    <span class="pickle-mark" aria-hidden="true">P</span><span class="name">The Pickle</span>
  </a>
  <p class="tagline">The news, explained by making fun of it.</p>
  <p class="supporting">Current events, reduced to their essential absurdity.</p>
</div>

<style>
  .masthead-wrap { text-align: center; }
  .utility-line { display: grid; grid-template-columns: 1fr auto 1fr; gap: 1rem; padding-block: 0.35rem; border-block: 1px solid var(--ink); font-size: 0.66rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; }
  .utility-line span:first-child { text-align: left; }
  .utility-line span:last-child { text-align: right; }
  .masthead { display: flex; justify-content: center; align-items: center; gap: clamp(0.7rem, 2vw, 1.2rem); width: fit-content; margin: 1.55rem auto 0.15rem; font-family: var(--serif); line-height: 0.88; text-decoration: none; }
  .masthead:hover { color: var(--ink); }
  .name { font-size: clamp(4rem, 10vw, 8.6rem); font-weight: 900; letter-spacing: -0.075em; }
  .pickle-mark { display: grid; width: clamp(2.6rem, 5vw, 4.35rem); aspect-ratio: 0.68; place-items: center; rotate: -6deg; border: 2px solid var(--ink); border-radius: 45% 52% 48% 54% / 30% 42% 56% 64%; background: var(--pickle-pale); box-shadow: inset 0 0 0 6px var(--paper); color: var(--pickle-dark); font-family: var(--sans); font-size: clamp(1.1rem, 2.5vw, 2rem); font-weight: 900; }
  .tagline { margin: 0; font-family: var(--serif); font-size: clamp(1.08rem, 2vw, 1.4rem); font-style: italic; }
  .supporting { margin: 0.15rem 0 0; color: var(--muted); font-size: 0.7rem; font-weight: 750; letter-spacing: 0.06em; }
  @media (max-width: 42rem) {
    .utility-line { grid-template-columns: 1fr 1fr; }
    .utility-line span:nth-child(2) { display: none; }
    .masthead { margin-top: 1.15rem; }
    .name { font-size: clamp(2.75rem, 14vw, 4.1rem); }
  }
</style>
